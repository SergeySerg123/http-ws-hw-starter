let rooms = [];

export default io => {
    io.on('connection', socket => {
        
        io.on('CREATE_ROOM', () => {
    
        });
    
        io.on('JOIN_ROOM', () => {
            
        });
    });  
};

 